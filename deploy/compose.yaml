services:
  tabletoprpc:
    build:
      context: ../kotlin
    container_name: tabletoprpc
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=60 -XX:+UseSerialGC"
